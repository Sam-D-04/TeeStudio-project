# Dependencies
node_modules/

# Env files (never commit real secrets)
.env
.env*.local

# Build output
.next/
out/
build/
dist/

# Logs
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# OS/editor
.DS_Store
*.pem

# Cấu hình MCP tool cục bộ (vd Playwright MCP) - không cần chia sẻ qua git
.mcp.json

# Tài liệu/ghi chú nội bộ, không phải source code
docs/
.claude/
.prompts/
HĐăng/
# File dữ liệu dump lớn, không phải source code
Database_main.sql
full_json_generated_data_vn_units.json
